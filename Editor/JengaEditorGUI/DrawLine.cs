using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

namespace Jenga {
    public static partial class JengaEditorGUI {
        // public static void DrawLine(
        //     Vector2 pA, Vector2 pB, Color color, float width = 1f
        // ) {
        //     var l1 = GUIUtility.ScreenToGUIPoint(pA);
        //     var l2 = GUIUtility.ScreenToGUIPoint(pB);
        //     // var ppp = EditorGUIUtility.pixelsPerPoint;
        //     // var scale = Matrix4x4.Scale(Vector3.one * ppp);

        //     // var w1 = scale.MultiplyPoint(l1);
        //     // var w2 = scale.MultiplyPoint(l2);

        //     Drawing.DrawLine(Vector2.zero, l1, Color.red, width, true);
        //     Drawing.DrawLine(Vector2.zero, l2, Color.green, width, true);
        //     Drawing.DrawLine(l1, l2, Color.blue, width, true);

        // }
    }
}
